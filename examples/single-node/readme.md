# Single Node AIC Orchestration Example

This example demonstrates the full AIC lifecycle on a single machine:

DIP → AdaptiveOS → AdaptiveAI → IBCS → Orchestration → Rollback

No containers.
No cloud.
Full control.
